<%= form_with(model: magzine) do |form| %>
  <% if magzine.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(magzine.errors.count, "error") %> prohibited this magzine from being saved:</h2>

      <ul>
        <% magzine.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
